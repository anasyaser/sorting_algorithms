#include "sort.h"

/**
 * merge - merge two array in sorted order
 *
 * @array: array of numbers to sort
 * @aux_array: addtional array to store sorted numbers
 * @l: lower bound of array1
 * @m: index of end of array1
 * @h: higher bound of array2
 */
void  merge(int *array, int *aux_array, size_t l,size_t m,  size_t h)
{
	
}
